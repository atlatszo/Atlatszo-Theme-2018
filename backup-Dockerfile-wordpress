FROM wordpress:latest

# installs memcached and enables it in apache
RUN apt-get update && apt-get install -y libmemcached-dev zlib1g-dev \
    && pecl install memcached-3.0.3 \
    && docker-php-ext-enable memcached

RUN rm -rf /var/www/html/wp-content/plugins/ \
    && rm -rf /var/www/html/wp-content/themes/ \
    && rm -rf /var/www/html/wp-content/uploads

COPY atlatszo/wp-content/plugins /var/www/html/wp-content/plugins
COPY atlatszo/wp-content/themes /var/www/html/wp-content/themes
COPY atlatszo/wp-content/uploads /var/www/html/wp-content/uploads

RUN chown -R www-data:www-data /var/www/html

# COPY docker-entrypoint.sh /usr/local/bin/

ENTRYPOINT ["/usr/local/bin/docker-entrypoint.sh"]
CMD ["apache2-foreground"]
